package bergCompiler;

/**
 * Character type classification
 * 
 * @author (sdb)
 * @version (Jan 2011)
 */
public enum TokenType
{
    KEY, ID, NUMBER, RELOP, ARITHOP, ASSIGN, SEMI, 
    COMMA, PAREN, THESIS, LBRACKET, RBRACKET, LBRACE,
    RBRACE, DOT, NOT
}
